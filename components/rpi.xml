<image>
	<profiles>
		<profile name="LiveInstallPi" description="Live Install ISO for Raspberry Pi">
			<requires profile="GNOME-Desktop"/>
			<requires profile="BootCore"/>
		</profile>
	</profiles>
	<preferences profiles="LiveInstallPi">
		<type image="iso" publisher="CentOS AltImages SIG" volid="CentOS_AltImage" primary="true" flags="dmsquash"
			filesystem="erofs" erofscompression="lzma,level=9"
			firmware="uefi" efifatimagesize="30" kernelcmdline="console=tty0 quiet rhgb acpi=off" mediacheck="true"
		>
			<bootloader name="grub2" grub_template="grub.cfg.iso-template" console="console" timeout="60"/>
		</type>
	</preferences>
	<packages type="iso" patternType="plusRecommended" profiles="LiveInstallPi">
		<!-- The point of a live image is to install -->
		<namedCollection name="anaconda-tools"/>
		<package name="anaconda"/>
		<package name="anaconda-install-env-deps"/>
		<package name="anaconda-liveinst"/>
		<package name="dracut-live"/>
		<package name="glibc-all-langpacks"/>
		<package name="isomd5sum"/>
		<package name="kernel"/>
		<package name="kernel-modules"/>
		<package name="kernel-modules-extra"/>
		<package name="livesys-scripts"/>
		<package name="rpi-release"/>
		<ignore name="gfs2-utils"/>
	</packages>
</image>
